# macOS metadata
._*
.DS_Store

# Media folders (hosted on Cloudflare R2)
5am in munic/
LDN x UKG/
Lunatic/
bents n glitches/
birthday kisses/
ftlotg/
hlfatpch/
lindsay lohan/
milano/
schimmel in schladming/
stoical/
tipsy triad/
debut/
valueME/
TOMIN INDEX.TXT/

# Claude Code temporary agent worktrees
.claude/worktrees/

# Misc
screen.png
DESIGN.md
reference.png

# Node
node_modules/

# Build output
dist/

# Test output
test-results/
playwright-report/

# Wrangler local state (cache, dev-mode artifacts, temp files).
.wrangler/

# Folder typo (renamed)
5am in munich/

# Journal game-asset working cache. Lives outside public/ so vite
# doesn't ship it into dist/. The real distribution path for these
# JPEGs / MP4s is R2 (uploaded by extract-frames + extract-clips). This
# cache only exists to speed up incremental re-extracts. Manifests
# (public/frames-pool.json + public/edit-colors.json) ARE committed.
.cache/

